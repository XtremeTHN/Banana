using Gtk 4.0;
using Adw 1;


template $SearchPage : Adw.Bin {
    child: Adw.Clamp {
        maximum-size: 700;
        child: Gtk.FlowBox mods {
            max-children-per-line: 3;
            row-spacing: 10;
            column-spacing: 5;
        };
    };
}