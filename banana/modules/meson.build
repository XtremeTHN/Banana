moduledir = pkgdatadir / meson.project_name() / 'modules'

modules_sources = [
    '__init__.py',
    'utils.py',
    'cache.py'
]

install_data(modules_sources, install_dir: moduledir)

subdir('gamebanana')