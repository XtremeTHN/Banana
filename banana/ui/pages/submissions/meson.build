moduledir = pkgdatadir / meson.project_name() / 'ui' / 'pages' / 'submissions'

modules_sources = [
    '__init__.py',
    'mod.py',
    'wip.py',
    'html.py',
    'download.py'
]

install_data(modules_sources, install_dir: moduledir)