moduledir = pkgdatadir / meson.project_name() / 'ui' / 'pages'

modules_sources = [
    '__init__.py',
    'home.py',
    'search.py'
]

install_data(modules_sources, install_dir: moduledir)
subdir('submissions')