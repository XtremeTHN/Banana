moduledir = pkgdatadir / meson.project_name() / 'ui'

modules_sources = [
    '__init__.py',
    'home.py',
    'search.py',
    'mod.py',
    'window.py'
]

install_data(modules_sources, install_dir: moduledir)
